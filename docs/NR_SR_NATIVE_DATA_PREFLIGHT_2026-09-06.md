# Nr3D / Sr3D 原生训练数据与预检输入

2026-09-06 18:06:51 CST，在 616 文件原生接入隔离源上完成标注范围审计。
证据：`refine-logs/native_joint_annotation_audit_20260906_v1/`。

| 数据集 | 训练语言行 | 辅助检测行 | 原生训练总行 | 单卡 B12/drop_last 更新数 | 正式验证行 |
|---|---:|---:|---:|---:|---:|
| Nr3D | 32919 | 11990 | 44909 | 3742，余5行 | 7899 |
| Sr3D | 65846 | 11990 | 77836 | 6486，余4行 | 17726 |

`joint_det` 在语言样本之后加入1199个有效ScanNet训练场景的检测提示，每个重复10次。
实际train pickle含1201个场景，原生检测加载器按已有规则排除两个索引，得到1199行。
训练和验证分别有1201/312个场景的superpoint文件，未发现缺失。
Nr3D语言训练/验证来自511/130个扫描，Sr3D来自1018/255个扫描；加入检测样本后，
两者的原生训练扫描与各自验证扫描均无ID交集，按下划线前缀定义的物理空间也无交集。
这不改变旧6172-row模块留出集已被冻结主干见过的证据性质。

审计调用当前原生标注加载器，保留Nr3D验证correct_guess筛选、Sr3D目标类别提及筛选、
对象类别检查、检测样本排除和缺superpoint过滤；只跳过语言图字段构建。
真实扫描对象标签已读取。没有构造点样本、运行GPU、更新模型或保存checkpoint。
CSV和split文件已计算SHA；大型扫描pickle只记录大小/mtime，没有声称本次重新验过完整SHA。

`preflight_rows.json`为后续实际GPU预检固定32条输入：每个数据集取原生标注顺序中
前12个不同扫描的第一条语言表达，再取前4条基础检测提示。选择不依赖模型输出；
保存原始语言/检测索引、原生joint索引和原始字段，SHA为
`3ff6ec4c359e2b00924073891cd5e6e3a6af33976e2bb963f4a01215978d2a45`。
这些是预检输入清单，尚未生成或通过实际点云/GPU预检。

ScanRefer正式底线通过后，实际预检应覆盖原生训练模式及增强、12语言行与4检测行、
zero-init输出一致性、新分支梯度、原生合法候选/selector输出、以及一次性更新。
预检权重不作为正式训练起点，正式启动需重新加载受保护预训练输入。
本次未确定Nr/Sr更新预算或使用旧长期训练脚本；表中每轮更新数对应B12、world_size1，
不能在更换batch或累积步数后继续当作相同的优化器步数。

后续Nr/Sr沿用现有磁盘保留机制，无需新增删除逻辑：

```text
--checkpoint_metric_retention
--checkpoint_retention_metrics rec_acc025 rec_acc050
```

该配置只保留本次run内的latest和两项REC单项最好，最多3个不同epoch文件；别名使用
已有hardlink，不复制大权重。新文件写入完成后才清理不再被保留的旧epoch。
执行前仍需为一次新checkpoint写入留出空间。保留单项最好不意味着可以把两个不同
checkpoint的指标拼成一行；晋级仍要求同一个模型满足两项REC要求。
Nr/Sr Mask不作为当前门槛或额外保留权重的理由；受保护历史结果和正在审计的Scan
端点按原规则保存。


## 已完成的真实输入检查：2026-09-06 18:22:08 CST

新的独立入口为 `scripts/run_native_candidate_local_preflight.py`。
其数据构造函数已在原Python3.7/Torch1.10.2环境实际执行：

- Nr3D、Sr3D各12条语言表达和4条检测提示，共32个固定原始输入记录。
- 每数据集分别关闭/开启原生训练增强读取两遍，共64份实际点云样本，耗时87.81秒。
- 每行50,000点、XYZ/RGB共6维；superpoint逐点对齐；对象输入132槽。
- 原生tokenizer、语言图解析、点采样和默认collate成功。
- 语言与辅助检测批次分别为12/4行；样本身份与20.93清单一致。
- `butd_cls`输入框及有效位逐位等于原生实例框/有效位，类别仍走预测类别路径。
- GPU前向、优化器更新、checkpoint写入均0。这不是模型效果或梯度通过的证据。

证据：`refine-logs/native_local_preflight_preparation_20260906_v1/receipt.json`。
GPU入口SHA：`50de01790a7e3b2a389e3257959cd67f852175f2949b86fa52e88dfd2dd734b8`。
本地编译与help检查通过，原环境CPU检查实际导入了相同入口。

待运行的GPU步骤仍需要真实Scan9508行结果先通过。条件启动脚本会从正式结果逐行
重算REC、Mask及原晋级条件，确认成对输入一致，再启动该预检；不会用准备阶段的
PASS代替正式晋级，也没有创建虚假的正式结果。

预检分别检查无增强eval输出逐位一致、原生selector分数/合法过滤/Query选择一致，
以及训练模式第一批零初始化输出一致。每数据集做两次一次性优化器更新：旧核心
LR1e-6、新分支LR1e-4，检查新输出投影及第二步的point/query/key/value权重梯度，
并确认冻结文本state未变。总计计划14次模型前向、4次一次性更新，完整checkpoint
写入0。Nr/Sr正式训练仍需重新加载其预注册起点；这里LR和步数只是小样本预检配置。

此次两个配置的加载预检使用兼容的Nr受保护权重。Sr历史最好权重仍未恢复，
这不代表已经测得Nr到Sr的迁移效果。当前Scan运行源码和配置保持20.88的固定版本。

