# 完整启动范围下的稀疏零残差梯度诊断

V2在原生预热后仍因共享GPU梯度范数逐位不等退出，0更新/0留出；缩小回归不足以证明修复。
本诊断逐行保留V2的全部模型/优化器/evaluator/32919条数据初始化以及首fit batch取得流程。
唯一操作为原生预热、原生检查、稀疏检查、原生重复、稀疏重复，共5次反向、0参数更新。
记录实际共享梯度张量、GPU范数、同输入hash及全部Box/score/原始Mask/alpha快照一致性。

预先区分：GPU标量范数归约差异、原生自身反向波动、稀疏路径共享梯度实质变化。
逐元素检查仍报告atol=1e-6/rtol=1e-5，既有范数精确检查也报告，不在诊断中放宽任何门槛。
controller0只表示采集完成；不会开始优化器或评估。完整初始化预计5–10分钟，首查600秒，
尚未结束则240秒一次。保留V1/V2原目录、失败栈和原始计划；新结果单独追加主交接。
