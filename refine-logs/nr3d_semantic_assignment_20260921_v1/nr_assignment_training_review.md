**Nr3D semantic-assignment paired-training review — 2026-09-21**

**Verdict: the current script is ready for the bounded optimizer preflight. No unresolved concrete source blocker found. Full-run execution readiness is not yet established; it requires the actual preflight PASS receipt.** This is a same-family provisional review, not a cross-family independent review. The reviewer made no source edits and did not launch training.

Reviewed `scripts/train_nr3d_semantic_assignment_pair.py` after its import, nonzero-gradient, and checkpoint-reload corrections, the previously reviewed assignment module, the canonical `scripts/nr3d_candidate_contract.py`, the dataset/evaluator/model call paths, and the downloaded native `fixed_source` loss/trainer/dataset sources. Also read the staging observation, saved E57 training configuration, and completed 16-row zero-update probe receipt. That earlier probe establishes loss integration on real tensors; it does not establish train-mode backward or optimizer behavior.

Three concrete issues were resolved before this verdict:

1. **Missing runtime candidate-contract import.** The initial script imported `scripts.nr3d_candidate_contract` from the selected `fixed_source`, but `nr_pair_stage.json` confirmed that file was absent there. The current script explicitly loads the staged sibling `nr3d_candidate_contract.py` with `importlib` (`train_nr3d_semantic_assignment_pair.py:38-42`). Its `models` imports still resolve to the selected native source. This removes the actual startup blocker without adding a fallback.
2. **Nonzero-gradient proof was missing.** The initial preflight accepted finite gradient tensors and a finite norm, including zero, and parameter changes alone would not distinguish a gradient update from weight decay. The current training loop requires a finite norm strictly greater than zero before every optimizer step (`:173-177`). It separately checks that each arm has changed native trainable parameters after the prescribed steps (`:199-201`).
3. **Reload claim initially exceeded the check.** Initially, checkpoint validation only deserialized a dictionary and compared its tensors. The current preflight first restores the initial model weights, strictly loads the saved model state, and compares every resulting state tensor (`:203-209`). It also constructs a fresh AdamW, loads the saved optimizer state, and compares its stored per-parameter state values (`:210-216`). Thus the receipt now has an actual model/optimizer state-loading check rather than only a file round-trip claim. It does not claim another optimizer step or an inference after reloading.

| Area | Assessment of the current implementation |
| --- | --- |
| Parent and architecture | Both arms are separately constructed with `TrainTester.get_model(args)` and strictly load the same evaluation-only E57 state (`:53-70`). No architecture replacement is introduced. The script intentionally starts fresh AdamW states; it is not resuming an absent E57 optimizer. |
| Parameter scope | The optimizer receives exactly the parameters whose native model constructor leaves `requires_grad=True`, identically named in both arms. It does not unfreeze every parameter indiscriminately. The model's native frozen text encoder remains frozen. Uniform `1e-5`, weight decay `0.0005`, and global clip norm `0.1` are the declared common adaptation choices. |
| Native losses | Both arms use `TrainTester.get_criterion` and the ordinary `_compute_loss`. The checkpoint configuration is retained, with explicit checks for selector supervision weight `0.5` and mask/consistency scales `1`. The replacement arm changes only the hooked final-layer CE; it does not replace the selector loss or apply a second decoder coefficient. In the inspected Nr3D native aggregate the CE coefficient is `1/7`. |
| Assignment binding | The replacement binds each current forward's `last_sem_cls_scores` before `_compute_loss` and requires exactly one record afterward (`:169-183`). The native arm has a distinct unmodified criterion. Evaluation calls the model and readout helper without calling the training criterion, so the training hook cannot modify evaluation scores. |
| Paired inputs and randomness | Each raw batch is transferred once and supplied to both arms. The script resets Python, NumPy, CPU Torch, and CUDA seeds to the same `2027 + step` immediately before each arm's forward. Both models enter train mode. First-step center, size, semantic logits, and protected selector scores are required to be tensor-identical before accepting the preflight. |
| Dataset and augmentation | Both arms use one Nr3D-only dataset with `augment=False`. The actual dataset derives joint detection from the supplied dataset dictionary, so no ScanNet detection-prompt rows are introduced even though the historical checkpoint configuration used joint training. Geometric/color/box-jitter augmentation is disabled in this path. Detector/GT-supported box inputs remain part of the existing protected Nr3D model protocol. |
| Full-pass accounting | The full dataset is required to have 32,919 rows. The shuffled loader uses batch size 4 and retains the final three-row batch; this gives exactly 8,230 steps. The final sorted `seen` check requires every row index exactly once. Each step records both losses, gradient norms, gradient-bearing parameter counts, and replacement selection counts/delta. |
| Optimizer preflight scope | It uses only eight existing fit IDs, performs two steps per arm, does no formal validation, checks first-step prediction identity, finite nonzero gradients, changed parameters, and model/optimizer state loading. The full-run path requires a passing two-step preflight receipt with the same trainable parameter names. |
| Evaluation readout | The helper uses the protected `selected_source_scores`, clamps predicted sizes as the native evaluation branch does, applies the same active-support-box overlap filter at IoU `> 0.25`, and selects the top valid query. The final metrics use the native strict thresholds `IoU > 0.25` and `IoU > 0.5`. Missing valid candidates count as misses. The canonical helper's sorting/filtering matches `GroundingEvaluator.evaluate_bbox_by_pos_align` for this fixed default query axis. |
| Evaluation population and schedule | Full mode requires exactly 7,899 validation rows and disjoint train/validation scan IDs. It first evaluates the parent/native model, trains the fixed one-pass pair without intermediate validation or selection, then evaluates both terminal models. It checks complete row order and identical baseline/terminal point inputs. No threshold search, best-epoch selection, or Mask gate is present. |

The memory behavior is bounded in the inspected loop: only one arm's forward/backward graph is active at a time; first-step prediction clones are cleared; per-step history contains Python scalars/lists rather than retained tensors; the assignment records are replaced each batch. Both model copies, their optimizer states, gradients, and the initial CPU state remain resident by design. Exact capacity must come from the optimizer preflight's actual peak allocation. The earlier frozen-model probe peaked at about 1.96 billion allocated bytes, but that cannot establish this training footprint. The full validation path uses batch size 16 and is not exercised by the optimizer preflight; it is executed before any update in the full run.

The saved recovery artifacts contain model/optimizer states and the step, but this script is a fresh-run launcher, not an implemented automatic resume path. The preflight checks checkpoint model/optimizer loading; it does not claim that a resumed dataloader/run was executed. This is a reporting boundary, not a request to add a speculative recovery framework.

The common protocol intentionally differs from historical training: fresh optimizer, uniform lower learning rate, no augmentation, and no joint detection-prompt rows. Any completed comparison must therefore be described as one-pass E57 adaptation under this shared protocol. It can compare the semantic-target intervention with its concurrent native-loss arm; it is not a reproduction of historical E57 training or an independent causal estimate from multiple seeds.

At this review's close, no optimizer-preflight result has been inspected. Proceed with the bounded preflight and assess its actual receipt before declaring the full run ready. Terminal full-run metrics must remain tied to the corresponding checkpoint, native architecture, protected selector/support-filter readout, and all 7,899 rows. No efficacy or benchmark improvement is claimed here.
