# 固定PV-Ground原生REC读取接口

本项为后续Nr3D/Sr3D接入提供可核对的原生分数、候选过滤与Query索引，不增加新的评分头或改动当前ScanRefer运行。
入口为 `scripts/pvground_rec_readout.py:native_root_rec_readout`，支持当前固定六层、256-token模型的最终root输出。
借鉴来源是固定PV-Ground提交 `262e2592589baec7bb83a0d46aae6542d4ccedfb` 的作者Evaluator；这是其决策操作的提取，不作为本项目新方法。

## 具体规则

- 输出原始框与按作者规则将尺寸clamp到至少1e-6的框；不原地修改模型张量。
- bbs使用soft-token概率；bbf使用温度0.07的Query/token对比概率并补零到256个token。
- 主实体map将正值置1，修饰/代词/关系/其他实体map保留实际值；原生分数为主实体＋修饰＋代词＋关系－其他实体。
- Nr/Sr `butd_cls`下，与有效对象框的最大IoU严格大于0.25时保留分数，否则乘0。继续使用Torch `argsort`排序256个Query。
- 单独返回原始分数、过滤后分数、对象重叠mask、两个模式的完整排序，避免离线报告与实际Query选择混用。

这里的乘零不同于旧MCLN selector诊断的负无穷屏蔽：存在负分候选时，零分Query仍参与排序。
当前接口原样保留作者操作，不根据这一结构差异更改阈值或候选规则。
目标GT框、GT Mask以及评估难度标记不参与该函数；它读取预测、现有文本成分map和协议对象输入。

## 已执行的CPU集成检查

2026-09-08 09:04:26 CST，在已验证的隔离runtime内执行 `scripts/verify_pvground_rec_readout.py`，正常退出0，用时8.66秒。
沿用Torch1.10.2+cu111和固定作者源码；此子进程屏蔽GPU，检查前后 `torch.cuda.is_initialized()` 均为False。
没有环境重建、权重读取、模型前向、参数更新或正式评估。

测试输入明确为合成预测与文本成分map：固定seed2027、每批4行、256Query、19个实际token、132对象槽，包含非连续对象有效位和需clamp的尺寸。
两种得分构造分别开启/关闭对象过滤，共四组。
通过Python逐行trace只读取作者实际BBox评估函数的局部变量，直接取得其分数和Top-10，不改写作者函数。

| 比较内容 | 实测 |
|---|---:|
| 两模式逐候选分数 | 8192个，逐元素相等 |
| 作者Top-10 Query索引 | 320个，逐元素相等 |
| 两阈值、Top-1/5/10的REC计数 | 48项一致 |
| 调用前后的输入张量 | 全部不变 |
| 移除GT框与评估属性后的选择 | 完全一致 |

负分构造中，合法候选均为负分，而乘零的无效候选被作者与新函数同时排到前面。这证明两种屏蔽操作确实不等价；它是协议案例，不是实际Nr/Sr发生率或新的错误统计。
本项未比较不同设备对完全相同分数的tie顺序，也未执行真实Nr/Sr网络；后续实际GPU评估仍需逐行与同设备作者结果绑定。

执行readout SHA：`3a4b77cbf41be2287f08f53f39f64ad44589a6932931f8e5895325fe88c4fefe`。
验证脚本SHA：`c73664e189deb2261fa1330b326026d095027d13db59094494eb383d96257400`。
证据目录：`refine-logs/pvground_rec_readout_cpu_20260908_v1/`，保留执行源码、固定spec、stdout/stderr、退出码和结果。

## 当前边界

运行中的ScanRefer训练及其已冻结正式入口继续使用原先已验证代码，没有热替换为此函数。
Nr/Sr正式模型加载、实际数据前向/反向和完整REC评估仍需Scan正式晋级后执行。
本项不改变三个基准的保护成绩，也不复活旧selector、局部模块或Mask专项。


## 独立 NumPy 对象过滤与计数核对（2026-09-08）

新增 `scripts/pvground_referit3d_rec_audit.py`，读取导出的原始框、两模式原始/过滤后分数、对象框及原槽位mask、实际选中Query与root GT。
对象重叠和选中框IoU采用独立NumPy float64几何计算；先核对过滤与最大分数选择，随后使用root GT计数。
尺寸先按模型float32规则clamp，过滤严格使用IoU>0.25；分数乘零，完全同分时接受实际Torch选择的最大值Query，不在CPU重排打破平局。

15:01:58 CST，已验证runtime的CPU子进程正常退出0，用时9.18秒。沿用同一组受控合成输入和固定作者Evaluator，核对8行、2048个候选过滤、16次最终选择，8项Top-1双阈值聚合计数与实际作者函数一致；原8192分数、320个Top-10索引、48项计数对照继续通过。负分构造保留作者的零分无效候选被选中行为。
另核对6个完整数据集命中数边界：Nr7899行4726/4059、Sr17726行12130/10157可过，任一阈值少1不可过。它们是人工边界测试，不是实测REC。Nr/Sr Mask不参与门槛。

这些合成输入不覆盖真实GPU全部数值边界、所有并列分数设备差异或完整数据集；真实模型输出、checkpoint绑定及完整正式评估尚未执行。此函数目前只核对输出packet，不声称独立重算了RoBERTa/网络语义证据。
没有GPU初始化、模型前向、优化器更新、下载新权重、运行源热修改或正式行。执行脚本、回执和退出码保存在 `refine-logs/pvground_referit3d_audit_cpu_20260908_v1/`。
