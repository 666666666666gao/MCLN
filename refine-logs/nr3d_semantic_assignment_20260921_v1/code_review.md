**Nr3D semantic-assignment code review — 2026-09-21**

**Verdict: no unresolved concrete blocking defect found in the current replacement, wrapper, or bounded probe.** This is a same-family provisional code review. It supports deploying and running the planned real-data, no-update preflight only; it is not a cross-family independent approval, a full-training review, or evidence of accuracy improvement.

Reviewed the current `models/nr3d_semantic_assignment.py`, `scripts/test_nr3d_semantic_assignment.py`, and subsequently added `scripts/probe_nr3d_semantic_assignment.py` in `C:/Users/gb/.codex_mcln_g0_20260905`, together with the actual native `SetCriterion.loss_pos_align`, `SetCriterion.forward`, `compute_hungarian_loss`, and the relevant dataset construction. For the real-data probe, also inspected the downloaded `fixed_source` runtime copies of `models/losses.py`, `main_utils.py`, and `src/joint_det_dataset.py`, plus its input manifest. Source and test files were not edited by this reviewer.

The replacement algebra agrees with the native objective. If `y = 0.6 * positive_map[0] + 0.2 * modify_positive_map[0] + 0.2 * pron_positive_map[0] + 0.1 * rel_positive_map[0]`, the native per-query term is `sum(y * log(y + 1e-6) - y * log_softmax(logits))`. The implementation subtracts the existing eos term and adds this exact root-target term, including its entropy constant. It does not normalize `y`. The difference retains the unmatched-query weight `0.1` and is divided by the incoming native `num_boxes`.

| Requirement | Evidence and assessment |
| --- | --- |
| Native token weights and entropy | `nr3d_semantic_assignment.py:37-46` agrees with `losses.py:1324-1351`. The eos entropy constant is also subtracted, so this is a replacement of the existing CE contribution rather than an additional positive term. |
| All matched queries protected | `nr3d_semantic_assignment.py:35-36` first selects geometric candidates, then excludes every query in the native Hungarian `query_ids`, including queries matched to non-root GT. The native matcher runs before the wrapper, in `losses.py:1711`; it is not changed. |
| Root and raw predicted boxes | The selector uses detached `outputs['pred_boxes']` and `target['boxes'][0]`. The normal native loss call constructs those predictions directly from the current prefix's center and size (`losses.py:3202-3207`). The dataset places the referring target first, before any anchor, and keeps it valid (`joint_det_dataset.py:1088-1117`); filtering by `box_label_mask` preserves this order (`losses.py:2297-2311`). |
| Strict IoU qualification | `nr3d_semantic_assignment.py:28-35` computes axis-aligned box IoU and uses `> 0.5`. The synthetic fixture includes an exact `0.5` candidate and confirms it is excluded. IoU remains a geometric selection proxy; it does not prove object identity. |
| Joint ScanNet prompts protected | Selection is skipped for `sample_dataset == 'scannet'`, even when its batch-level `language_dataset` is `nr3d`. This distinction matches the actual dataset: `language_dataset = self.test_dataset`, while `sample_dataset = anno['dataset']` (`joint_det_dataset.py:1264-1272`, returned at `1402-1403`). |
| Native denominator retained | The wrapper receives `num_boxes` directly. Native `forward` computes it from all matched target indices and performs the existing distributed reduction (`losses.py:1725-1737`). The module does not substitute batch size or the number of selected queries. |
| Final-layer CE only | `bind` retains the exact `last_sem_cls_scores` object and the wrapper uses object identity (`nr3d_semantic_assignment.py:61-74`). The native aggregation passes the same tensor object through `pred_logits`, without cloning it (`losses.py:3205-3206`). Other prefix tensors therefore retain their original CE terms in this call path. |
| Native aggregate coefficient retained | The wrapper adds its delta to the native `loss_ce` before the existing per-prefix sum (`losses.py:3225`). The normal aggregate applies `weight / (num_decoder_layers + 1)` (`losses.py:4057-4082`); `weight` is `1` for Nr3D. The wrapper must not add another decoder coefficient. |
| No additional geometry-gradient path | Predicted boxes, root boxes, and token labels are detached in the delta. The synthetic autograd assertion confirms no delta gradient to its box tensor. This concerns the replacement delta only; native box/mask objectives and gradients through shared model parameters remain part of ordinary training. |

The latest inspected `nr_assignment_synthetic_execution.json` reports process exit `0`, empty stderr, and a passing synthetic receipt. Its explicit relabel comparison has loss error `1.1181278125604877e-11` and maximum logit-gradient error `2.5404152826508097e-10`. The fixture selects exactly `[batch 0, query 2]`; it covers matched-root and matched-other-GT protection, a ScanNet row with otherwise qualifying geometry, an Nr3D row with no eligible unmatched query, the strict IoU boundary, zero delta gradient outside selection, detached geometry, last-logit identity gating, hook removal, and an all-ScanNet zero-delta case. The criterion CE method used by that test is extracted from the actual repository source. I read the execution receipt; I did not independently rerun it locally because the default local Python launcher fails before import with `No pyvenv.cfg file`.

The synthetic test calls the extracted native CE method directly. It does not execute the full native `forward`, the complete decoder aggregation, the real dataloader, or an optimizer step. Denominator provenance and aggregate scaling were verified from their current source in this review. Their execution-level confirmation remains the planned real-data preflight. This remaining integration check is an evidence boundary, not an identified source defect.

**Resolved probe finding:** the initial probe used `0.5 / 7` as the expected native total-loss coefficient. That would be wrong for this Nr3D runtime: both the workspace source and the downloaded `fixed_source/models/losses.py:4052-4054` set `weight = 1`, changing it to `0.5` only for ScanRefer. The parent corrected `probe_nr3d_semantic_assignment.py:95` to `1. / (args.num_decoder_layers + 1)` before this final verdict, and I reread that change. The assignment module was unaffected because it already delegates this coefficient to the native aggregation. No unresolved change is requested for this finding.

The reviewed probe loads the manifest's existing model source and evaluation-only checkpoint, freezes parameters, calls `eval()`, and performs the network forward under `torch.no_grad()`. It selects the first 16 existing fit-row IDs, constructs only the Nr3D train dataset, disables augmentation, and uses four batches of four. The current manifest supplies 2,048 fit row IDs; its first 16 are the ones passed to the dataset's annotation-selection override. It does not create an optimizer or request a formal evaluation split.

The probe then marks only the final logits/center/size output tensors as gradient leaves. It obtains the actual native targets, matches, and denominator through a temporary capture around the native CE method; runs the ordinary `_compute_loss` first without and then with the intervention; requires exactly one intervention record; checks native aggregate scaling; constructs a dense explicit relabel objective and compares both its value and logit gradient; checks zero delta gradients outside selection and no delta gradient to center/size; and finally compares every model state tensor with the loaded checkpoint state. It requires at least one selected query overall, so an entirely empty intervention cannot yield a passing receipt.

This is an appropriate bounded witness of the actual criterion call path. It does not include real ScanNet rows (that exclusion is covered by the synthetic fixture and source review), train-mode forward/backward through model parameters, or optimizer updates. A successful receipt would establish those stated integration properties only. At the time of this review, I have reviewed its source but have not seen its real-data execution receipt.

No remaining source changes, fallback logic, or new defensive branches are requested. Full training, checkpoint selection, and efficacy claims remain outside this review.
