# Sr3D真实零尺寸对象输入修复验证

全训练输入裁剪审计完成：1018扫描、34865有效对象槽，0空裁剪，但有两个对象框三个
尺寸均为0：scene0561_01/object21和scene0673_05/object89，各裁到1个真实采样点。
旧模块的正半尺寸断言会拒绝这些实际输入；原审计的失败receipt保留，不改为PASS。

最小修复只定义零长度轴的归一化坐标为0：该轴裁剪保证坐标等于中心，除数设为1。
正长度轴仍除以实际半尺寸，包括极薄正尺寸。box_crop_mask、框范围、输入点和RGB均不变；
不加epsilon、扩框、补点、实例标签清洗或数据集分支。参数仍为41472。正在运行的Nr3D
配对目录保持旧模块SHA4b3dadf6a25508a3453e81416e415b4262cd11fd044ab3324ab76b20d0b54f9b。

本地7测试和原服务器Py3.7/Torch1.10的相同7测试已通过。真实输入检查固定读取原审计
Sr3D过滤后的训练行18112、21602，augment=False，使用原50000点与原生RGB。
两扫描的点、框、有效槽和裁剪计数逐项对齐原审计；所有正尺寸对象的MLP实际输入必须
与旧归一化精确相等，两个单点对象的归一化位置为0且保留实际RGB。

先检查零初始化输出，再将输出矩阵固定为0.001倍矩形单位矩阵，检查有效非零外观，
仅由各异常对象输出构造固定随机线性损失，要求5个参数张量梯度非零且有限，随后恢复
原参数并清空梯度。总计4次外观CPU forward、2次CPU backward、0优化器更新、0原生
MCLN forward、0GPU、0正式评估行。不能将此检查写成Sr3D性能实验或权重恢复。

前后核对锁定源码、数据、原FAIL receipt、CPU测试证据及运行中Nr模块；使用AST确认
裁剪函数未变。仅两条已观测异常需要新特征检查，不重复完整1018扫描审计。
入口scripts/probe_sr3d_zero_extent_object_inputs.py；原服务器CPU两线程独立目录。
按同类数据加载估计约3分钟，首次检查180秒后，若仍在运行后续240秒。
