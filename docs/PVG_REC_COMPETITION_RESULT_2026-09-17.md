# F：原生 REC 竞争监督固定终点

F 已完成，独立完整性复核通过，双 REC 不退化筛选失败；9508 条正式评估按预设规则跳过。Mask 不参与此次筛选。这里所有准确率和候选分析均来自6887条主干预训练见过的模块留出表达，不是正式未见场景成绩。

## 实验与原始结果

保持 D 的完整预训练 PV-Ground、六源观测状态与任务读取，唯一训练改动为原生 bbs 的 root 匹配候选竞争损失。seed2027、相同官方epoch81父、batch8、LR/backbone LR1e-5，29778行恰好遍历一次、3723步。训练用时8999.150秒，23:07:12结束；终态6887条推理705.690秒，23:18:58结束，整体controller于23:19:02正常退出。没有选中间权重、改学习率、追加轮次或修改推理规则。

| 固定输出 | Acc@0.25 hits | Acc@0.50 hits |
| --- | ---: | ---: |
| D/F共同起点 bbs | 6147 | 5549 |
| D终点 bbs | 6136 | 5547 |
| F终点 bbs | 6150 | 5501 |
| F相对自身起点 | +3 | -48 |
| F相对D | +14 | -46 |
| F起点 bbf（诊断） | 6175 | 5584 |
| F终点 bbf（诊断） | 6155 | 5506 |

F主输出从89.2551%/80.5721%变为89.2987%/79.8751%。D/F初始rows、boxes、scores完全相同；两臂3723步训练行顺序一致。对照脚本重新绑定spec、source、初始及终态receipt/导出SHA，逐行核对表达ID、场景、目标、点SHA和root框。新增竞争没有新增网络参数，不能将此差异写成结构容量对照。

相对D，@0.25修复186、破坏172；@0.50修复328、破坏374。374个严格破坏中，271个仍在(0.25,0.50]，103个降至0.25及以下。不能仅据此认定前271个语义身份正确，因为选择和框都发生了变化。

23:23:45独立审计通过：每个快照重计1763072个候选框、13774次实际选择，终态最大所选IoU误差2.99e-6。完整fit覆盖、原生与竞争损失求和、实际竞争次数均通过；eligible25/50为29593/28401，active25/50为26410/23872。工程有效不意味着性能有效。固定terminal SHA为9762fb3e91ed1203f085328b4388ed45e74a5e8b920b9aac7c3a499cd298f4ca。

## 候选覆盖与选择分开重计

| 快照 | raw256 oracle@0.25/@0.50 | 有合格框但没选中@0.25/@0.50 |
| --- | ---: | ---: |
| F起点 | 6872 / 6696 | 725 / 1147 |
| D终点 | 6879 / 6701 | 743 / 1154 |
| F终点 | 6852 / 6687 | 702 / 1186 |

这里是全部原始256框、原生尺寸下限下的GT上界，没有新增合法过滤，不能称为部署准确率或合法候选召回。

F相对D的严格阈值净变化可描述为 **-46=-14-32**：上界少14条，有合格框但未选中的差距多32条。这是集合计数恒等式，不是将46条逐一因果分配给回归器和评分器。

374条严格破坏中，仅8条在F的256框内没有合格框；其余366条仍有合格框，其中171条最佳合格框排第2，170条在第3—16，23条第17—64，2条第65—256。对应子集没有分数并列。全体F严格失败中，合格框存在的1186条最佳合格排名中位数为6，D对应为4。

因此，不能把F解释为单纯候选生成退化，也不能宣称它已经修复最终选择。继续全局增大相同margin或加训练轮数缺乏本次证据支持。

## 标注框最大重叠代理

使用此前核验过的真实场景对象框，对D/F实际所选框独立重计；逐行点SHA和root框一致，所选IoU与导出误差小于1e-5。

两臂所选框均以root为唯一最大重叠对象的6093条中，@0.25修复66、破坏65，净+1；@0.50修复237、破坏273，净-36。其余样本严格阈值净-10，共同构成-46。相对F自身起点，同类代理子集严格阈值净-38，其余净-10。

该代理支持继续区分目标附近的范围质量和明显跨对象变化，但不证明真实语义身份，也没有分开“改选较差框”与“同一个实例的回归变差”。动态Query不能永久按编号对应。不能据此宣称Mask冲突、某个分支梯度冲突或唯一根因已被证明。

第一次对象归因脚本在最后计数校验时遇到变量名遮蔽，正常退出码1且未写结果。修正分析脚本的局部变量名后，完整CPU重计exit0；初次源码和错误日志保留。未改训练、导出或筛选口径。

## 后续工作边界

本版F封存为有效负结果，保留终点供必要的固定输入机制检查，不扫描权重、margin或学习率。原V99正式58.6033/50.4523不变；F没有新的9508正式成绩，也不能以其89.30/79.88模块留出成绩替代正式主表。

下一项有针对性的检查应在预先固定的训练场景上，分别记录当前Hungarian root匹配、最高bbs候选与最佳几何候选：比较最终评分实际读取的token证据及回归质量，核实竞争监督是否对当前高质量候选提供了有效区分。需要重新执行当前模型才能取得的匹配和token证据，不由本次保存的box/score导出推断。该检查尚未运行，未据此启动下一份训练。

后续结构或目标改变应明确解决上述差距中的哪一部分，并保留原生GT控制；不能将本次F换名重新训练。Scan正式双REC达到现行保护线后才执行已准备的Nr/Sr同结构训练，Mask不阻挡，三数据集目标仍未完成。

证据：refine-logs/pvground_scanrefer_finetune_20260917_rec_competition_v1、对应endpoint_audit/formal目录，以及refine-logs/pvground_rec_competition_comparison_20260917_v1。全部分析0模型forward、0optimizer、0新增正式行；独立候选和对象归因结果保留在selection.json与overlap/summary.json。

23:35:25已核实所有原controller正常退出。Nr/Sr单batch检查和完整串行训练均按Scan REC失败跳过，0新模型forward/optimizer/训练任务；不是仍在后台运行。
