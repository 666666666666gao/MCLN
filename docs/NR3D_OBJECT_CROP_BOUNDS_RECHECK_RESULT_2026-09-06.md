# 显式AABB裁剪复核通过；原生接入尚待执行

仅将对象外观的点归属改为显式lower/upper边界比较，没有增加容差、扩大框或补入
其他点。5项原服务器CPU测试PASS，其中实测scene0054_00对象79案例确认纳入
点16592；另一原实例点在当前float32显式边界外，仍未纳入。

完整511场景/16181有效对象槽复核完成，controller.exit=0，输入合同PASS：

| 检查 | 结果 |
|---|---:|
| 空裁剪 | 0 |
| 非正尺寸框 | 0 |
| 点、框、对象索引与v1相同 | 全部 |
| 重算旧abs裁剪点数与v1相同 | 全部 |
| 模块Torch predicate与NumPy显式边界逐点相同 | 全部 |
| 边界点归属发生变化的对象槽 | 11697 |
| 新增/移除点成员次数 | 16858 / 4271 |
| 新裁剪点成员总数（含对象框重叠） | 34211533 |

更换比较式会影响多个对象的边界点，不能描述成只改了一条样本。上述数量是
对象—点成员次数，并非不重复场景点数，也不是新增扫描观测。
计时186.781秒不含数据集初始化；0原生模型/GPU forward、0参数更新、0正式验证行。
结论限定于已锁定的无增强Nr3D训练输入，不外推其他基准或增强后的框。

原型当前SHA：`4b3dadf6a25508a3453e81416e415b4262cd11fd044ab3324ab76b20d0b54f9b`。
复核receipt SHA：`4b86d7119cf22866ed3830559949496cac3425facdc434955c68d6a165029a72`。
旧原型及v1审计保留在Git 7b3e371和原远端目录，未覆盖原始失败证据。

原生Decoder预检代码与固定16行/12forward计划已独立staging，原Py3.7编译PASS。
输入清单`7451d43c0644a03888f73c919ceea0cc4bb11570bfd69215bec32c48e3b377e9`。
该预检尚未执行；等待C1完成终态并按其固定筛选决定GPU顺序。
后续检查新增外观是否改变末层Query及两种语义路径、是否连接原生REC主项和完整loss，
同时保持前五层Query及采样不变。没有对象外观训练或质量结论。
