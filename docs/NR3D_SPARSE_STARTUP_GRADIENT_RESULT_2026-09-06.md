# 稀疏局部记忆启动数值检查：完整诊断与V3

V1/V2均在共享GPU梯度范数逐位相等断言处退出，均为0优化器更新、0留出评估。
V2增加一次预热仍未解决完整启动问题；旧目录、计划、错误栈及原始数值全部保留。
这两次退出是工程检查失败，尚不能据此判断稀疏局部记忆的质量。

完整范围诊断于07:04观察到controller0、进程已结束、GPU1MiB。保留V2全部32919行
数据初始化、两臂模型/优化器/evaluator构建顺序，同首批4条fit输入；依次原生预热、
原生检查、稀疏检查、原生重复、稀疏重复，共5次反向、0更新。

| 比较 | 最大绝对梯度差 | 最大每张量相对L2差 | 原逐元素检查 |
|---|---:|---:|---|
| 原生检查→原生重复 | 3.59416008e-5 | 1.93891754e-5 | FAIL |
| 原生检查→稀疏检查 | 2.23517418e-8 | 3.74046848e-8 | PASS |
| 原生重复→稀疏重复 | 2.98023224e-8 | 3.99067523e-8 | PASS |
| 稀疏检查→稀疏重复 | 3.59416008e-5 | 1.93891753e-5 | FAIL |

5次Box/score/Query Mask/Text Mask/alpha前向和loss均逐位相同，loss=16.18368148803711。
较大差异位于x_mask梯度，10个rel_encoder/x_query共享张量相同；所有原参数、buffer、
输入点hash均未变化。差异也发生在预热后的重复反向，不能继续称为确定的冷启动问题。

27,001,909-byte实际梯度文件SHA为
`ed5443c99d908c14a97b54bc286646864bdcedbfa8b83599df4d25805b2ef7b8`。
独立CPU读取后逐项复算与回执一致。尝试解释用的atol1e-5/rtol1e-4逐元素检查仍不能
覆盖原生重复差异，该失败也保留；没有把这个检查冒充通过。

V3将检查对象明确为每张量梯度向量相对L2误差，阈值固定1e-4，约为已观测原生上限
的5倍。实际5组×16张量共80个比较通过；对16张量分别施加1%相对扰动全部拒绝。
原逐元素atol1e-6/rtol1e-5与GPU范数精确性保留描述项；删除未奏效的预热。
检查回执先落盘，再断言；不满足则退出，不自动重试或继续放宽。

这是工程数值标准的明确修订；mIoU相对原生终点和受保护起点均至少+0.2个百分点、
Mask@0.25/@0.50不下降的科学门槛、6687步、学习率、损失、输入点与全部结构都不变。
当前源码group_points_grad_kernel包含浮点atomicAdd，与累加顺序敏感相符；未直接
替换该kernel做因果实验，不能宣称已确定它是唯一根因。TF32/原CUDA运行设置未改。

V3于07:17:29启动PID28416，目录/root/autodl-tmp/mcln_sparse_point_pair_20260906_v3，
manifest `ade6fe609cbadf0930e4f82f4f7031495450197637e2013aca5ef7b0441ff09e`。
原Python3.7八项测试、612源码/1026数据、16/33张量实际参数计数与梯度实物回归均通过。
训练前完整首批数值检查尚待实查；07:27:29首查，07:37:29读取真实进度估时，之后
接近预计阶段结束再以240秒观察。初始和终态6172行、同样本/候选/输出规则不变。

当前尚无训练后质量、正式Nr3D/Sr3D/ScanRefer新结果。该实验只验证Mask局部证据，
不声称已经改善复杂表达、共享最终Box/Mask身份或完成三数据集目标。
