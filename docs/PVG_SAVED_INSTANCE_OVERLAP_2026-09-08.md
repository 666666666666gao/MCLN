# PV保存输出的实例几何对应诊断

本次为CPU保存输出诊断，0模型前向/更新/新正式行，不改当前训练、晋级规则或候选选择。复用已核验的OpenShape诊断scene_geometry，而非重新加载大场景pickle；旧6887场景标注框与本次PV原生起点、原生终点、empty-pool零更新三份输出逐行核对，三份均6887条点云SHA一致、目标框重建差≤1e-6米，最大分量差4.5036024e-7米。输入文件与复用脚本SHA固定，输出独立重算root IoU并核对每条两阈值与原receipt命中完全一致。全体标注对象含嵌套/结构类别；同类表示原始instance_label字符串相同。

### 原生bbs主输出：失败几何对应

| 快照 | REC命中25/50 | @.25失败：root唯一最大/同类其他/异类其他/并列/无重叠 | @.50失败：同顺序 |
| --- | --- | --- | --- |
| 正确VSA原生起点 | 6147/5549 | 153/502/78/4/3 | 724/519/88/4/3 |
| 同预算原生终点 | 6119/5537 | 162/498/100/3/5 | 702/526/114/3/5 |
| 空邻域屏蔽零更新 | 6138/5549 | 157/509/77/4/2 | 720/526/86/4/2 |

@.25起点740条错误中，502条与同类其他标注框唯一最大重叠；@.50起点1338条错误中，724条仍与root唯一最大重叠。说明两个阈值的几何对应分布不同。该最大重叠代理不证明语义身份，不能把502全部命名为模型认错实例，也不能把724全部命名为回归器错误。

### 已结束原生训练的配对分解

同一表达两次输出都以root为唯一最大重叠对象，共6114条。其@.25修复51/破坏68，净-17；@.50修复247/破坏238，净+9。全体分别156/184净-28、332/344净-12。因此严格阈值的算术分解为 **-12 = +9（双root子集） -21（其余样本）**。其余样本严格阈值修复85、破坏106；其中原root→其他同类83、→其他异类21，另有异类→异类2条破坏。此处是已观测分组的修复/破坏统计，不是因果贡献。

这次不能把OpenShape“多数净损失位于双root子集”的具体结论直接转移到PV原生训练。PV仍有目标附近严格范围失败，但本次原生严格净退化没有主要集中于这个双root子集。也不能据此说只需改选择器：核心与框均更新，两次选中Query并非固定对应，此分析仍不能独立区分重新选框和框回归变化。

空邻域屏蔽这里只分析零更新结果：对原生起点@.25净-9、@.50净0；其中6251条双root子集分别-5/+4。完整训练终态尚无结果，不将零更新几何分布当作训练收益，也不因小量跨进程数值变化过度归因。bbf完整记录仅为诊断，不替代预设bbs主输出。

### 当前运行接续

19:42 CST附近实时观察：原训练controller20870、endpoint审计20878、正式接续20981、CPU跨控制分析21426均仍为原进程存活，无terminal receipt。训练1152/3723、累计2745.49秒，磁盘余2295664640B（约2.14GiB）。按当前吞吐预计训练加6887终态约21:30—21:45 CST；正式9508仍需先通过独立终态审计和本轮原始bbs两阈值不退化筛选。当前没有新正式成绩，不提前启动Nr3D/Sr3D，不修改固定seed、LR、Gumbel或总步数。训练权重未删除；本次仅新增小型CPU分析产物。

限制：6887为预训练曾见场景的模块留出；不是未见场景泛化证据。原生/完整V99不混报。三数据集完整目标未完成。
