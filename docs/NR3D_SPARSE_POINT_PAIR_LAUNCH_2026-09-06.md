# 稀疏局部记忆全fit配对：已启动，终态待定

原始点—稀疏体素局部记忆的16扫描原生预检PASS后，已固定独立学习计划并启动服务器
配对进程。此记录没有新的正式成绩，尚不能判断该分支是否有效。

启动前原Python3.7/Torch1.10环境的8项测试全部通过（5项质量/身份统计规则、3项点映射），
612份冻结源码、1026份数据文件、parent、受保护起点逐行参考和稀疏运行回执均核验。
原生16个参数张量的实际checkpoint形状合计1348960；新分支构造器17张量267936；
sparse共33张量1616896。没有将手工估算作为启动依据。

本次使用既定全部26747 fit表达/413扫描，holdout6172表达/98扫描；无增强、同实际
batch、一个epoch、B4（末批3条），两臂各6687次更新。既有Mask层学习率1e-5；
新局部分支1e-4；fresh AdamW、weight_decay=.0005、clip_norm=.1，原损失不变。
两臂其余权重、buffer、REC候选及选择路径冻结。预算对两臂相同，但sparse多19.86%
可训练参数，且新参数有独立学习率，因此不是等容量对照。

固定质量门：sparse终点同时相对native终点和受保护起点，Mask mIoU至少+0.2个百分点，
且Mask@0.25和@0.50均不下降。另报固定Query及起点好框Query条件下的融合Mask质量。
holdout是底层主干见过、已多次用于模块筛选的训练场景，不是全系统新场景泛化证据。
质量门/训练步数不根据中间质量更改；结束后单独复算逐行结果和实际参数/AdamW状态。

06:09:20启动screen mcln_sparse_point_pair，实际PID27206，启动前GPU1MiB。
06:11:21观察PID仍存活，GPU3029MiB，日志处于完整训练数据文本解析初始化；当次尚未
出现首批梯度preflight或基线评估记录。后续脚本会先完成首批损失/梯度一致检查，再
完整重现6172行起点，全部通过后才能执行第一次优化器更新。不能把进程已启动写成
训练或初始评估已经完成。

初步预计单卡A100约3–5小时；首次阶段观察06:29:20，用实际批次耗时修正估计，再
接近预计结束前五分钟观察，其后240秒间隔。只用耗时安排监控，不依中途留出质量改计划。
本地observer已安排，运行日志与不可变回执由collector保存；没有新增邮件或消息通知。

运行目录：/root/autodl-tmp/mcln_sparse_point_pair_20260906_v1。
manifest SHA：`aa32b731f0e105a1dbd218a9f9edfb2c1919981340694577b9c12ed1edc99c05`。
入口scripts/run_nr3d_sparse_point_pair.py；独立终态复算scripts/summarize_nr3d_sparse_point_pair.py。
完整固定计划docs/NR3D_SPARSE_POINT_PAIR_PLAN_2026-09-06.md；版本化计划/执行表与证据
位于refine-logs/sparse_point_pair_20260906_v1。历史G0计划及已失败O1/B/C1/L1保持封存。

三个数据集受保护正式结果不变，完整目标仍active。此轮Mask局部记忆本身不更新REC，
如果通过，仍需独立验证新证据到身份推理的连接及正式基准收益。Sr3D受保护权重恢复
问题仍按原记录处理，没有用Nr3D权重替代Sr3D来源。
