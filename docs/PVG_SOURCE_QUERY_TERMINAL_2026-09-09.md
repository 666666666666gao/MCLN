# 普通分源Query完整终态与下一项观测状态对照

普通六源Query读取B已完整结束。固定3723次更新、29778条fit完成；2026-09-09 01:12:03完成6887条终态评估（711.57秒），01:12:07训练receipt完成，原controller25348退出0。seed2027、batch8、单pass、LR/backbone1e-5、原生损失及预设bbs输出未更改；未挑选512/3584步快照或追加预算。

### 终态结果：主REC未通过

| 输出 | 相同零更新起点25/50 | 同预算native终点 | B固定终点 | B对自己起点 | B对native终点 |
| --- | --- | --- | --- | --- | --- |
| bbs主输出 | 6147/5549 | 6119/5537 | 6136/5481 | -11/-68 | +17/-56 |
| bbf诊断 | 6174/5583 | 6153/5550 | 6158/5512 | -16/-71 | +5/-38 |

bbs对自己起点@.25修复158/破坏169，@.50修复319/破坏387；对同预算native终点分别修复155/破坏138、修复278/破坏334。后者334个严格破坏中256个终框仍>.25，78个降至<=.25。它说明需要继续区分实例判别与范围质量，但不能仅据IoU区间认定真实实例身份或回归器是唯一根因。

bbs Mask为6168/5801，mIoU73.97063835%；相对自己起点-6/-23、mIoU-0.17374347个百分点；相对native终点+2/+1、mIoU-0.06006799个百分点。bbf为6180/5812、74.08481890%，仅诊断。模块留出场景被预训练见过，以上不能替代9508正式ScanRefer结果，也不能宣布正式Mask保护达标。

CPU跨控制比较逐行核对6887条row_id、scan_id、target_id、点SHA、root box；3723步训练行顺序相同；实际初始rows/boxes/scores文件完全相同。原生与B使用各自已固定的源目录，差异就是已归档分源读取接口，不要求不同目录字符串相同。全量保存候选原框GT oracle：native终点6865/6684，B终点6865/6692，差0/+8；B自己的起点6872/6697到终点为-7/-5。这是未合法过滤的raw256上界，不是可部署命中率，也不充分证明scorer为唯一问题。

新增714528参数、24个state tensors的普通分源读取已得到具体负结果。它既不支持“普通六路attention已经有效”，也没有测试观测状态或语义/几何不同读取。不能把本轮失败改名后重跑，也不能扩展成所有点—体素、多源或解耦方法无效。

### 独立审计、正式接续与磁盘

独立审计通过：完整3723步、29778条fit恰好一次、冻结项未变、保存候选与分数SHA、逐行选中框IoU和metrics均复核。Mask检查范围仍为导出行IoU重计，并非重新加载二进制Mask。正式接续按预设bbs双阈值规则写入skipped_primary_rec_regression，0条新增正式评估、0项Nr/Sr训练，不为了进入正式而切换bbf。

在训练、独立审计及正式接续均退出0之后，仅删除已由3723终点替代的3584步latest.pth。删除前核对精确路径、实际step、父权重/模块/spec SHA及终点SHA；删除后再次核验terminal。没有删除受保护V99、官方三数据集预训练或本轮固定终点。具体释放字节与剩余空间见latest_cleanup.json。

### 最新用户研究约束与下一项C

用户要求沿点—体素多源与语义/几何分工继续，下一项保留内容并显式提供真实观测状态，不恢复empty-pool硬清零。状态附着于真实FPS支撑位置、来源、半径；包括真实邻居存在、去填充重复的支持数、距离及范围状态。两个半径分开；BEV按自身投影意义处理；不把非零响应当真实观测，不把候选中心空当坏框，不把FPS状态冒充连续GumbelQuery中心覆盖，也不强制空源权重为0。

C作为独立最小机制对照：以与B相同官方父权重、输入、训练顺序、seed2027及固定预算建立零更新一致起点，再加入观测状态条件化读取。不能从失败B终点继续训练后声称同预算B→C增量。先核对状态来源和真实支持定义，再固定实现与参数/计算预算；当前C尚未实现、尚未启动、没有性能结论。保持原生GT损失，暂不加入新的质量/蒸馏/Pair/CDF/边界分布或扩大体素范围。语义—几何任务读取D仅在C有正证据后再做。

当前正式保护仍为ScanRefer V99 58.6033/50.4523（5572/4797 of9508），Mask59.8443/52.3349/45.9303；Nr56.6527/47.5883，Sr68.4813/58.3042。Scan达V99 REC不退化与Mask58.70/50.70/44.72即转Nr/Sr REC，不等59/51全达；Nr/Sr Mask验收继续暂停。固定单seed、不做长周期baseline复现或seed/LR/epoch搜索。总目标未完成。

独立审计完成时间：2026-09-09T01:16:24.467822+08:00；终点SHA：ec3a08674ccaa648ce808626b88411cf5d06df7c6e64f1a116171b36ffd1d1e8。
清理释放339774806字节；清理后空闲1759318016字节。
