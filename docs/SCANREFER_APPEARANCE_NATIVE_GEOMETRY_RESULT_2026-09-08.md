# ScanRefer 预训练对象外观：原生框的几何归因

## 结果与范围

2026-09-08 05:34:20 CST，CPU 分析完整结束。复用固定外观配对三臂各6887条原生终态预测，覆盖106个训练场景；没有重新前向、训练或访问正式验证集。这些场景此前被旧主干训练见过，绝不是新场景泛化成绩。

本次将预测行号映射回原始36665条ScanRefer训练表达，核对scene、原始target_id、root框和全部50000点XYZ/RGB的SHA。每个选中框与该场景全部已标注对象框比较，原始类别标签保持，不剔除结构或嵌套对象。使用已有分类函数：root唯一最大重叠、其他同标签对象唯一最大、其他不同标签对象唯一最大、并列和无重叠；并列容差保持1e-6。

**最大框重叠是几何对应代理，不是语义身份真值。** 本次没有把Query编号或IoU>0.25当作实例身份。完整V99行文件没有保存最终框，所以不能将这个原生归因表当作完整系统各阶段的归因。

## 严格阈值失败分布

| 原生REC@0.50失败的几何代理 | 保护起点 | 同预算GT控制 | 预训练外观 |
|---|---:|---:|---:|
| root为唯一最大重叠对象 | 701 | 514 | 556 |
| 另一同原始标签对象为唯一最大 | 188 | 192 | 197 |
| 另一不同原始标签对象为唯一最大 | 42 | 37 | 35 |
| 并列 | 0 | 0 | 0 |
| 无重叠 | 1 | 0 | 0 |
| 失败总数 | 932 | 743 | 788 |

外观相对同预算控制多45条严格失败，其中root唯一最大组多42条、另一同标签组多5条、另一不同标签组少2条。这个总体类别差分与下方配对结果不是同一统计。

## 配对结果

| 外观相对控制，原生@0.50 | 修复 | 破坏 | 净变化 |
|---|---:|---:|---:|
| 两臂均root唯一最大重叠 | 95 | 139 | -44 |
| 其余几何对应组合 | 36 | 37 | -1 |
| 全部 | 131 | 176 | -45 |

176条破坏中的139条（约79%），两臂都更贴近目标标注框；这一组的净变化为-44，几乎构成全部-45的严格净损失。该结果支持继续重视目标附近的范围质量，不支持把损失全部解释成新增外观语义认错类别。它仍不能区分框回归变差与改选同一目标附近的低质量Query，因为旧记录只存实际选中框，没有完整256个框及其配对。

@0.25全部修复64、破坏58、净+6，其中两臂均root唯一最大组修复20、破坏17、净+3。这里不能因为宽松阈值小幅正收益，就忽略严格阈值损失。

完整系统相对控制的169条@0.50破坏中，外观模型的原生框有133条仍属于root唯一最大组，其中103条原生本身已未过0.5，30条原生已过线；其余36条的原生框更贴近其他对象。注意这里描述的是完整系统破坏样本里的原生几何，未重建缺失的完整系统最终框。

## 验证与判断

原Python3.7环境和本地10项测试均通过，涵盖非连续实例编号、目标/Query区别、坐标误配、并列、无重叠和配对计数。随后本地使用独立标量AABB公式重算20661个选中框、762882次预测—对象框比较：全部类别、目标编号、两个阈值和修复/破坏计数一致，最大IoU差2.583741437911513e-6。所有源文件、训练点数据、注释和原预测SHA后检不变。

当前固定外观方案继续封存，不将本分析解释成通过，也不重新扫描其学习率、投影容量、辅助权重或epoch。历史G0增强配对已在§20.37完整结束并封存，保留增强修复代码，不存在待补跑的同一性能实验。

后续转向核验完整预训练几何主干/解码器及其实际输出协议，优先使用可获得的ScanRefer预训练组件减少训练成本。旧的局部采点、质量头、教师框转移、Mask几何和冻结读出实验均已有结果，不能换名重复。采用外部代码和权重必须区分上游成果与本项目新结果；当前保护V99、Scan Mask底线、Scan先行且达线即Nr/Sr REC的目标不变。

## 证据

- 原生终态：`refine-logs/scanrefer_object_appearance_pair_20260908_v1/native_completion/`。
- 本次分析：`refine-logs/scanrefer_appearance_native_overlap_20260908_v1/`；summary SHA `72fab673dfafe3bd4936188fdc48a61082b02e9d75a7bf82534c04de6f7ce5b1`。
- 几何和逐行结果：`overlap_rows.json.gz`，SHA `35ae606aa4f170400b056cd3bcf228e2df9deb673073dafa1f436ecbe275c503`。
- 分析入口：`scripts/analyze_scanrefer_appearance_native_overlap.py`，复用既有`analyze_scanrefer_instance_overlap.py`的固定几何分类。
