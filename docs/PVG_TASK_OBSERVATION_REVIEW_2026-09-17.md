# D任务读取：审查与工程证据

设计参照1229025；核心实现提交ea69722。按implement/code-review技能，分别使用Spec与Standards两个独立只读审查轴。审查不代替运行证据，不证明定位性能。

## 两轴结论

- Spec轴：核心模块、三处源接口、固定训练/审计接线、正式恢复/评估接续、A/D及B/D及C/D CPU对照均无实质阻断问题。
- Standards轴：核心及训练、权重清理、观察、正式恢复/评估文件无阻断或需处理的规范问题。三个正式脚本经过compile语法检查。
- 观察器沿用既定契约：训练、审计、正式接续三个目录及launch.json存在后使用；未增加缺目录fallback。
- `task_read_by_arm=False`表示父臂任务读取残差禁用，不表示`layer.task_read=False`。父臂reader.enabled=False仍返回两个零残差，继续走共享eval尾部，保证tuple接口。

## 实际已完成检查

接口目录：`refine-logs/pvground_task_observation_interface_20260917_v1/`。

2026-09-17 12:08 CST完成真实CUDA单元和完整模型fixture检查。使用4条训练表达、2次临时原生更新，新增165888参数、读取模块923616参数、37个新增state。第二步语义/几何task矩阵梯度范数分别约0.000189899/0.000615244，两者均更新。训练参数820张量、28883227参数，冻结文本参数保持。严格CPU序列化恢复1271项，无权重文件写入。

CUDA单元在非零输出映射下检查task读出的独立变化与梯度；共享dropout尾部与原C在训练/评估态相同，RNG相同。真实fixture的9项REC张量在两批中一致；Mask张量存在约1e-5至1e-4数值差，不宣称全量输出逐位一致。batch2峰值约5.50GiB，不替代batch8检查。

正式接续CPU准备于12:17 CST通过：37项合成变化全部严格恢复、缺失项被拒绝；1234父项扩成1271项。独立重算过去6887行和1763072框通过。该合成fixture不是训练终态，也没有新正式推理。

## 固定执行边界

D从同一官方ScanRefer epoch81初始化，不使用C终点续训。batch8容量backward不更新，随后恢复完整初始state及RNG；先6887零更新，后3723步固定fit，再6887终态。原生损失、数据划分、seed2027、LR1e-5、bbs主输出保持。没有新质量监督或多seed。

只有自身bbs双阈值不退化且独立审计通过，才进行真实终态CPU恢复及两臂各9508正式评估。目标仍为V99 5572/4797及ScanRefer Mask三底线。A/B/C/D模块留出结果均不能作为完整系统新场景泛化。
