# PV空邻域控制完整终态与跨控制比较

本轮empty-pool控制已完整结束，不再处于等待状态。2026-09-08 21:22:28完成固定3723次更新/29778条fit，训练8841.16秒；21:33:50完成6887条终态，耗时682.67秒。原训练controller20870退出0；21:36:41独立审计通过；正式controller20981按预设主REC退化规则跳过，0条9508正式评估、0项Nr/Sr新训练。21:41:07 CPU跨控制比较也完成，controller21426退出0。没有修改原seed2027、学习率、预算、输出或中途选择epoch。

### 完整终态：科学门未通过，执行完整性通过

| 原生输出 | 自己零更新25/50 | 固定终点25/50 | 自己训练变化 | 同预算native终点 | 终点相对控制 |
| --- | --- | --- | --- | --- | --- |
| bbs（预设主输出） | 6138/5549 | 6154/5514 | +16/-35 | 6119/5537 | +35/-23 |
| bbf（诊断） | 6171/5585 | 6163/5532 | -8/-53 | 6153/5550 | +10/-18 |

bbs自己@.25修复176/破坏160，@.50修复342/破坏377；377个严格阈值破坏中284个终框仍>.25。对同预算native终点，@.25修复171/破坏136，@.50修复302/破坏325。两轮3723步训练行顺序完全一致，6887条row_id、scan_id、target_id、point SHA及root box逐行一致。初始两臂差-9/0，终态差+35/-23，因此训练变化之差+44/-23仅为描述性差分，不能把初始差算作训练收益，也不声称独立因果归因或跨进程逐位确定。

bbs Mask命中6167/5819→6185/5827，mIoU74.06608854→74.25583175（+0.18974321个百分点）；相对native终点74.03070634为+0.22512541个百分点。它是模块留出分割局部正信号，不抵消主REC严格阈值退化，不用于宣布正式Mask达线。bbf Mask6194/5834及74.35269209仅诊断。各模型输出与V99完整系统分开，不拼接单项最好。

原始全部256框GT oracle：native终点6865/6684，candidate终点6860/6686，差-5/+2；candidate自己的零更新6873/6698→6860/6686，差-13/-12。这是未做合法过滤的raw全集上界，不能命名为合法候选recall或部署性能。严格oracle略高于控制但实际选择更差，也不足以单独证明错误只来自scorer，因为框与Query同时更新。

独立CPU审计每阶段重算6887行、1763072框及13774次输出选择检查，终态root IoU最大差2.79969149e-6，命中与receipt一致。Mask项复核导出行IoU而非重载二进制Mask重新前向。759个允许训练的参数张量实际改变，冻结项保持，完整3723步预算核对通过。工程完整性不等于科学成功。

### 几何对应诊断：严格损失集中于双root代理子集

继续复用已核验真实scene_geometry，以点SHA、root重建及每行IoU对齐四份保存输出；0模型forward/更新/新正式行。candidate终点@.25共733错，几何唯一最大重叠分类为root144/同类其他492/异类其他90/并列5/无重叠2；@.50共1373错，为739/521/106/5/2。

candidate自己的initial→terminal：6120条双root唯一最大重叠子集，@.25修复59/破坏58净+1，@.50修复247/破坏279净-32；总严格净-35=-32（双root）-3（其余）。native终点→candidate终点：6110条双root子集，@.25修复63/破坏47净+16，@.50修复217/破坏242净-25；总严格净-23=-25（双root）+2（其余）。这支持进一步区分候选选择与目标附近范围质量，不宜把本轮退化概括为明显类别转换。最大重叠只是几何代理，不是真实实例身份；两臂选择不固定、框也更新，仍不能区分重选低质量框与回归变化。旧20.196原生训练的分解+9/-21继续保留，不因本轮结果改写。

### 归档、磁盘与后续边界

在独立审计和正式跳过确认之后，21:38:33只删除已由3723终点替代的3584步latest.pth，释放331178334字节（约316MiB）。删除前确认精确目录、原训练退出、终点存在、step和architecture flag/module SHA、文件SHA；终点再次核验保留。磁盘空闲1901510656→2232688640字节，约2.08GiB。没有删除任何受保护父权重、Nr/Sr官方预训练或固定终点。cleanup脚本和执行回执归档，不重复执行。

终点331181662字节，SHA e5cc9aa891e5386b74fe2c9d6390f2ce3935cf18e986c0144b1808b06f42e773；audit SHA 6a7134e83d83418c5e97e18ea545f9aa16719ed69f25f890b9858c11ceb3cb76。所有训练、审计、正式接续和CPU比较已结束，不再轮询或重启它们。该零新增参数、池化后清空空邻域的具体版本封存为负结果；它不否定真实覆盖差异，也不证明复杂覆盖权重会有效。

下一项沿既有研究计划，先实现普通分源多尺度Query读取作为控制，并与随后覆盖条件版本使用相同读取位置/记忆/预算比较。保留完整语言及全局原路径，从真实融合前分源特征读取，不能将最终框中心是否为空当成候选质量标签，也不能将Gumbel混合的关键点mask冒充新坐标处实际覆盖。下一版本不加载失败终点，不自动继承本轮empty-pool=True，不扫描本轮LR/epoch。具体新增结构、参数/步数先固定并验证，再启动Scan；当前尚无下一结构训练或性能结果。

正式保护成绩仍为Scan V99 58.6033/50.4523及Mask59.8443/52.3349/45.9303，Nr56.6527/47.5883、Sr68.4813/58.3042。Scan正式REC5572/4797、Mask58.70/50.70/44.72门不下降，正式通过即推进Nr/Sr REC；不恢复Nr/Sr Mask验收门或多seed。6887场景曾被预训练看到，不作为新场景泛化证据。三数据集总目标未完成。
