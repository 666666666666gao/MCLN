# EG-3DVG ScanRefer权重迁移到Nr3D：固定零更新起点

2026-09-20。前置ScanRefer完整验收已完成：last/bbs5542/4952 of9508，即58.2878/52.0825。该权重epoch69、SHA785f46ca595aed3a06efdfc46dc1c4b0493b212ccb66062a76f519324fd24f7e，继续原样保留。V99受保护5572/4797不变。

## 本次执行

- 完整EG同一作者ScanRefer权重，在Nr3D7899条正式测试表达上做零更新迁移；不是Nr3D专用权重复现，不是训练后结果。
- 固定seed2027、batch8、完整6层作者模型、last/bbs主输出；bbf仅诊断。先8条完整前向，随后全部7899条，最后独立CPU计数。8条不作精度筛选，不改变固定权重/seed/主头。
- 使用独立root `/root/autodl-tmp/mcln_eg3dvg_nr3d_transfer_20260920_v1`；controller4408于17:51:17启动。固定spec SHA fcae845cc495120fe5e48bbf0a69022242089d0e5881fbfca5f4d4d3d9738cae。
- 复用已实际通过完整EG前向的Torch1.12+cu116环境，canonical SHA81a835e8144f7070d66feb0afa460911dec610052ced6b96c98097352cd022fd未变；不重建环境、不重复启动ScanRefer。

## 作者输入与评分协议

`butd_cls=True`：模型读取全部场景GT实例框及作者预测类别，目标身份/目标框/文本角色标签不作为model.forward的额外输入。不能把这条路径称为完全不使用GT对象框。

主评分沿用作者规则：先生成回归框与预测Mask包围框的平均框；与任一输入场景对象框IoU>.25的候选保留原分数，其他候选分数乘0。严格保留乘法和排序规则，不改成负无穷mask。没有按root目标是否命中来过滤候选。

同一次forward另记bbs_unfiltered/bbf_unfiltered，仅取消上述分数乘法，输入和框不变。这是候选过滤诊断，不是去掉GT对象框输入的单阶段模型。两种路径都与原生GroundingEvaluator逐batch核对，禁止事后选更高路径作为主结果。

完整候选导出(N,256,17)：6回归框量、6平均框量、2过滤分数、2未过滤分数、1对象支持位。每行另存实际对象框，独立CPU重建IoU资格、过滤乘法、所选框和命中数。只用GT进行已有作者协议和明确离线计数，不将离线oracle当实际精度。

## 训练准备与下一步

CPU进程4480独立调用未改动作者load_annos，为Nr3D train及ScanNet检测样本生成缓存，核对真实场景/目标/superpoint。另记录作者方向词判断与同词表规范分词之间的差异；这是输入检查，尚未修改增强、未训练。

作者train_nr3d.sh默认240轮、骨干LR1e-3；本项目不直接启动这一长周期流程。main_utils.load_checkpoint会恢复旧epoch并尝试恢复optimizer/scheduler，且使用strict=False，不能原样用于此处跨数据集初始化。后续有限适配必须严格加载完整模型、重新建立优化器、独立记录更新预算，并保留完整作者损失/输入语义，不能称作作者240轮原生复现。

先取得本轮真实迁移起点和全量训练缓存，再固定一次有限适配的预算。尚未启动优化器或训练，不把ScanRefer完成误写为三数据集目标完成；不把旧PV队列套到EG，不同时加C/D/G或新的重排模块。


## 17:58:35执行状态

8条完整前向17:52:02已通过，所有模型状态不变；预检命中仅作执行记录。正式评估已记录1536/7899条、314.17秒，controller4408和GPU9110MiB实际存活。预计18:20—18:25含CPU复算完成，尚无完整Nr精度。训练CPU缓存仍在解析；训练Dataset预检由4557在缓存exit0后自动执行。
