{
  "first_preflight": "Stopped before upload/launch: source manifest was looked up in preparation root instead of model_source/native_source_manifest.json. Existing preparation script established the correct path.",
  "controller_exit": 1,
  "runner_stop": "KeyError topk_per_source: protected Parent stores both budgets in candidate_rule, as used by train_dist_mod._build_rec_reranker_outputs_float32.",
  "dataset_constructed": false,
  "model_forward_count": 0,
  "optimizer_steps": 0,
  "checkpoint_writes": 0,
  "correction": "Use the existing nested candidate_rule fields in v2; source model, protected weights, fixed row identities, budget and decision rules are unchanged. No fallback added."
}
