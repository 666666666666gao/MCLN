# 真实点—体素局部记忆：原生预检PASS

完成16条fit/16扫描、B4的12次原生MCLN forward，0优化器更新、0留出/正式评估。
本结果证明输入映射、接入和梯度路径可用，不是Mask或REC精度提升。

新增代码直接编码同一份50000点XYZ/RGB，以2厘米体素构建32/64通道的细尺度稀疏
邻域路径；通过每点inverse映射回原始点，与连续点残差拼接，按实际superpoint成员
求均值后，以零初始化64→288投影加入原Mask输入。原PointNet++全局路径保留。
没有使用SA1插值，也没有输入额外扫描点、GT实例Mask或新对象框。

原Python3.7的3项CPU测试全部通过。实际构造器计数267936参数/17张量，与原生回执
一致；所有612源码、36数据文件、parent、M3实际点hash和已验证运行环境回执保持锁定。

- 零残差时：seed、Query、Box、score、原Query/Text Mask及alpha逐项精确相同。
- 零残差的输出矩阵梯度范数4.3454–17.4574，其余16张量梯度为0，符合零投影结构。
- 固定0.001矩形单位投影后，4批原Query Mask最大logit变化0.04953–0.06061；
  全部17张量原生Mask损失梯度有限非零，REC及Decoder Query仍精确相同。
- 结束恢复全部新参数；原模型所有参数/buffer、源码、数据和parent均未修改。

## 实际输入与资源

16扫描合计800000个采样点、680786个占用体素，每扫描范围23138–47379。
全部点有逆映射ID，RGB逐点保留，XYZ重建最大误差1.43051147461e-06米。
有5个扫描出现体素跨多个superpoint：合计5624体素/12952点。
这表示超点边界混合，不是GT实例污染率；统计仅覆盖固定16扫描。连续点分支和原超点
归属仍保留，不把量化或逆映射描述为恢复新观测。

4批原生no-grad forward约0.258–0.376秒；新增分支autograd forward约0.403–0.611秒。
两者梯度模式不同，不能把该比值当作部署推理开销。完整预检段33.668秒，不含数据集
初始化；Torch allocator峰值2170812416 bytes（约2.02GiB），不覆盖库自行分配的全部显存。
原MCLN TF32设置保留为true，与独立合成算子测试的TF32关闭设置分开记录。
05:54:19启动PID26956；首次180秒观察05:57:19已经controller0、进程结束、GPU1MiB。

manifest SHA：`8c15d1ef9dd2c6c2c85fffd548d7acc34880f61cc9e512e133ecf11d9c01cdc2`。
receipt SHA：`3e6a308f1a476ec37c79125373d49d1fb0ce1540f38cbb8b40586ecaa384afda`。
证据目录：refine-logs/sparse_point_preflight_20260906_v1。
入口：scripts/run_nr3d_sparse_point_preflight.py。

下一步固定同起点、同数据的原生Mask与新增稀疏局部记忆学习对照。计划使用完整26747
条fit表达一个epoch，让新建局部编码器接触全部既定fit场景；具体优化器和验收设置
在独立计划固定后才启动。当前尚无此分支训练更新。它只影响Mask，后续是否连接目标
身份推理须看新证据；不能以该预检替代Nr3D/Sr3D的正式REC目标。O1/B/C1/L1封存不变。
