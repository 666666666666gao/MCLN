# Nr3D D-task interface preparation

Prepared only; no GPU launch or queued execution. Requires the actual D formal9508
audit and advance_to_nr3d_sr3d_rec before taking the shared GPU lock. Uses the fixed
official Nr epoch25 parent and existing eight-row training fixture (four referring,
four joint detection). Preserve butd_cls protocol and original native Mask losses.
The D reader is installed after strict1235-state parent load;1272 full states,
820 trainable tensors. One train-mode forward and backward, zero optimizer steps,
verify finite losses/gradients and unchanged parameters, restore all buffers.
Record task-query gradient norms without requiring nonzero at zero-output start.
No accuracy, learned task separation or full dataset capacity claim.

Run in the existing runtime environment after the formal gate, as a file so
__file__ identifies the checked script. No launch or automatic gate change here.
