# 覆盖感知读取：后续研究候选及实际接入位置

用户于2026-09-08提出以正确点—体素接口为基础，检验Query级覆盖状态与语义/几何读取分工。当前状态为研究候选，不是已实现网络或成功实验。正在运行的ScanRefer固定训练不热修改；先完成它的终点与既定评估。后续仍先Scan，正式达当前底线后及时Nr/Sr，不等待59/51。固定seed2027，不增加多seed或跨seed集成。

## 已从源码与权重清单核实的入口

当前VSA在1024个raw-point FPS关键点处聚合各源，再拼接经过Linear(1024,288)、BatchNorm、ReLU。源码实际拼接顺序不同于YAML中列表的位置；不能按配置列表顺序盲切通道。

| 源 | 融合前通道切片，左闭右开 | 三维邻域半径/米 | 每半径槽位上限 |
|---|---|---|---|
| BEV | 0:128 | 高度压缩后的二维插值 | 不适用 |
| raw_points | 128:256 | 0.1 / 0.2 | 16 / 16 |
| x_conv1 | 256:384 | 0.1 / 0.2 | 16 / 16 |
| x_conv2 | 384:512 | 0.2 / 0.4 | 16 / 32 |
| x_conv3 | 512:768 | 0.4 / 0.8 | 16 / 32 |
| x_conv4 | 768:1024 | 0.8 / 1.6 | 16 / 32 |

预训练清单中`module.backbone_net.vsa.vsa_point_feature_fusion.0.weight`真实形状为[288,1024]。融合前特征的输出语句目前是注释，现有Decoder获得的是融合后表示。若研究分源读取，应保留融合前的真实分源特征及其几何关联，而非把同一个融合后288维向量再过几个MLP。这是拟议插入点，不声称当前已经修改。

证据：固定YAML、修正后`pv_utils.py`、官方Scan父权重的CPU参数清单；三者SHA与切片记录见`refine-logs/pvground_coverage_source_map_20260908_v1/receipt.json`。这是源码/配置/权重结构映射，0前向、0更新、0正式行；它没有测量真实局部覆盖率或新方法收益。

## 必须区分的观测状态

- VSA关键点位置与最后预测框中心分别记录。当前关键点来自原始点FPS，不能先假定raw路径在这些点上经常完全无邻居；候选回归后的局部范围是另一种查询位置。
- 原始输入点在不在、点是否进入体素范围、某尺度是否存在邻居、最终选取了多少唯一邻居，分别记录。槽位数16/32及重复补齐不能冒充真实邻居数。
- BEV是高度压缩的二维表示。XY处存在特征不等于目标Z范围有三维观测，不能和各三维半径邻域共享未经区分的占据标签。
- 非空与有判别力不同；有效点数、最近邻距离是观测描述，不是预测IoU或语义正确标签。特征零值也不能直接当空邻域标签。
- 稀疏卷积邻居中心与其感受野不同。某个中心没有落在GT实例内，不足以证明该特征没有看见目标。GT只用于训练监督和独立错误分组，不能进入推理覆盖特征。

## 最小研究顺序

1. 当前固定训练完成后，先判断修正接口下的原生与正式结果，保持预训练来源、输入、更新预算与两阈值口径。
2. 新版本先比较原读取与普通多尺度读取；它们共享冻结起点、数据、seed、预算及最终bbs规则。普通读取本身来自已有方法思想，作为控制。
3. 在相同多尺度读取结构中，分清“仅屏蔽真实空邻域”和“额外使用Query级观测状态”的贡献。不能把二者同时改变后的收益全部归于学习式覆盖建模；不是现在追加两份训练。
4. 只有覆盖读取表现有支持，再验证全局语义/关系与局部几何读取不同记忆的增量，继续共享候选身份。首版不同时改骨干、语言编码器、matcher、Mask全部路径或增加自由最终分数残差。

参数与输入预算需在新运行开始前固定；本文不指定尚未验证的模块宽度、loss权重或训练步数，不根据正在运行的结果中途调参。现有VSA投影可作为预训练初始化来源，但将各路归一化为和为1的权重并不自动等价于原拼接投影，不能声称零更新行为必然保持。任何新路径都要实际核对起点输出与原预训练的差异。

## 可检验主张与边界

主要性能要求仍是Scan REC不低于同一V99，Scan Mask不低于既定baseline；Nr/Sr REC不低于各baseline，Mask不额外拦截。记录两阈值修复/破坏、候选oracle、输入支撑分组、时间/显存和参数，不能只报告覆盖模块权重图或loss下降。

潜在贡献是语言条件下利用空间局部的有效支持与任务职责分工，不是首次点—体素融合、多源注意力或任务Query。新增机制的新颖性与性能均未证明。VSA批次排序、检测框增强顺序与mesh数据修正是正确实现的基础，不能包装成网络主创新；V99正式成绩不能贴到这套尚未训练的结构名下。

## 独立CPU支持观测工具（2026-09-08）

`scripts/pvground_support_observation.py`只读取一个源、一个半径的导出NPZ，计算同batch三维中心距离；不接入模型或训练。字段为support_xyz[N,3]、support_batch[N]、query_xyz[Q,3]、query_batch[Q]、selected_global_indices[Q,K]、selected_valid[Q]。索引必须由导出端转换为support_xyz全局行号，不能直接传入stack算子的batch局部索引；selected_valid保留算子原空邻域掩码。实际模型导出端尚未接入，真实场景覆盖统计仍待当前固定训练完成后执行。

输出分别记录全部半径内支持行、不同坐标数量、最近同batch支持距离、分配槽位、有效选择槽位、唯一选中行/坐标、跨batch与半径外选点。空邻域的[0,0,...]不作为有效支持读取；无同batch支持时最近距离为null。支持行或精确不同坐标仍不等于独立传感器观测，无法从这些数组恢复被上游重复采样的原始点身份。

距离采用CPU float64、严格小于半径；这是几何审计，不保证与CUDA float32半径边界逐位等价。BEV不进入这个三维格式；体素中心支持不是卷积感受野覆盖，非空也不是语义有效或定位质量标签。该工具不检查原始点是否进入体素范围，也不检查目标框内点数；这些须与既有输入审计和后续实际框范围统计分开关联，不能把中心邻域计数当框覆盖率。

五个已知几何查询覆盖交错batch、重复槽位/坐标、空邻域、无同batch支持、半径边界及错误batch选点；另有两个查询经过NPZ命令行读取并检查JSON。证据在refine-logs/pvground_support_observation_cpu_20260908_v1，包含输入、输出、执行源码和SHA。全部为人工几何夹具，不是模型实测、GPU算子一致性证据或新方法收益，0前向/更新/正式行。

### 实际stack算子导出约定补充

已只读当前运行环境的OpenPCDet pointnet2_stack CUDA与Python源码。ball_query逐batch按计数前缀访问支持行，返回batch内局部索引；第一个有效邻居填满槽位，再按遇到顺序覆盖，达到nsample立即停止，不是最近K个。无邻居时CUDA令idx[0]=-1，Python BallQuery保留empty_ball_mask后把该行idx设为0。随后QueryAndGroup把空行的grouped_xyz和grouped_features清零，但仅返回new_features和idx，丢弃empty_ball_mask。因此不能从QueryAndGroup返回的全0索引、或最终特征是否为0推断空邻域；后续导出应截取ball_query返回的原mask。

新辅助函数stack_indices_to_global按真实支持/Query批次计数把局部索引变为全局行号，并让empty mask决定有效性。导出数据仍需保留坐标实际batch ID；不能只由前缀计数重建“正确batch”标签，否则会掩盖曾经发现的交错行错误。四个不等长双batch合成查询验证局部0在第二batch的偏移、重复0与空行的区别；原五个几何夹具同时通过。尚未对真实模型执行采集。

当前VSA在grouping后清空无效坐标和特征，不能直接套用旧MCLN Mask未屏蔽seed0的案例。源码证据在refine-logs/pvground_support_stack_contract_20260908_v1/source_contract.json，记录上游来源和每个实际文件SHA；只读源码，不修改或重编译扩展。空行在后续MLP/BN后的值也不能替代原empty mask。

### 真实采集已排队，尚未执行（2026-09-08 16:21 CST）

`capture_pvground_support.py`复用已有固定输入与三次前向replay脚本。只包装首次PVGround前向，在VSA聚合入口保留真实坐标batch标签，在ball_query返回处保留empty mask和局部索引；转换后抽取每样本固定步长32的关键点（1024中取32），不读取GT/分数来选样。raw和conv1—4的两半径共10个包，各256个关键点，同时记录全部8192个关键点的算子空邻域数。它不测BEV、最终候选框内覆盖或三数据集总体比例。

包装调用在finally中恢复，仅限新诊断进程。第二次同seed原始前向须与首次被观察前向的既有全套trace逐项精确一致，否则停止并保留失败，不能宣称观察无影响。第三次按原replay递进RNG用于原协议控制，不新增seed或参数更新。实际点坐标NPZ和reference_trace留在服务器诊断目录，observer只收集JSON/log，不把原始场景点云上传GitHub。

同一环境spec966235b2...、官方Scan父权重及原8条冻结输入复用，不重新加载数据采样、不重编译、不新建预训练副本。远端Python3.7语法检查通过，不等于模型前向通过。队列/root/autodl-tmp/mcln_pvground_support_capture_20260908_v1，controller17222/queue17223已在16:21:30核实存活；首次17:25检查现有Scan formal controller14358正常退出，随后每300秒。执行时持相同GPU锁，至多一次三前向采集，失败不自动重试。当前只有CPU等待，0实际采集前向/更新/正式行。后续用scripts/observe_pvground_support_capture.py读取同一进程，不重复launch。


## 实际采集结果与位置边界（20.190）

实际支持采集17:30:27完成、controller/capture退出0：使用官方Scan epoch81、固定8条输入，仅3次forward、0更新、0正式评估；观察与不观察的同seed重放张量一致。十组来源×半径分别记录8192个VSA关键点，实际operator空邻域全部为0。每组固定256点的CPU复核未发现串batch、半径外选点或重复填槽。该结果不支持在此批次VSA关键点处宣称普遍空邻域。

输入receipt核实8条表达全部属于scene0003_00，不是8个独立场景。进一步只复用已保存reference_trace及完整支持坐标，校验trace与每个packet/report SHA；按每行候选索引0,8,...,248取32点，合计256点，未按GT、得分或缺失情况筛选。CPU float64几何检查中，最终候选中心的原始点0.1米邻域107/256为空，0.2米51/256为空；x_conv1对应109/50；x_conv3的0.8米和x_conv4的0.8/1.6米均0空。此项0模型forward、0更新、CUDA未初始化，属于假设在候选中心读取时的几何支持，不是实际执行过的候选特征读取或新模型成绩。中心落在物体内部可能没有表面点，不能据此判断实例不可见；粗尺度非空也不保证语义正确。关键点和候选中心的差异支持后续在训练场景多样本上审计读取位置，但尚不构成覆盖机制的有效性证据。

17:35:53按用户磁盘清理授权，仅删除本轮已被3723步终点替代的3584步latest.pth，释放331178206字节。删除前校验流程退出、独立负结果审计、旧进程结束、父权重和终点SHA，删除后重验terminal保留；磁盘空闲2814865408→3146043392字节。终点SHA仍6b951587f09674ec441b30d8c30627d94c6bdca24a8a4d9bee274cd9a3248c2f。未删父权重、终态或Sr传输文件。清理receipt及脚本归档，无训练动作。

Sr同一PID13440继续传输；17:42:49远端.part=670924800/829830168字节，约80.85%。本地完整长度/SHA已通过，远端尚未最终校验，CPU inventory/strict-load未完成。按当前速度预计17:47左右传完，再检查同一任务；不重启、不提前启动Nr/Sr训练。Scan当前已封存负结果，9508正式评估按预设跳过，现有V99正式最好保持。


## 16训练场景与最小mask控制（20.191）

Sr官方权重传输17:51完成，829830168字节及SHA a4a14b0090947177a648703ad6de094246891d89174fffa56fe454f730dbe3dc在两端通过。17:51:57 CPU清单完成，实际epoch31、butd=False/butd_cls=True/butd_gt=False、joint_det=True；1235状态张量，与Nr清单schema一致，比Scan多text_encoder.embeddings.position_ids。随后CPU严格加载通过：1235张量逐项一致、missing/unexpected均空，783可训练张量27959611参数、199冻结张量。GPU未初始化，0模型forward/更新/正式行，不能声称Sr模型成绩。启动器在screen启动后出现进程列表断言错误，保留原启动源码与错误；未重启任务，按实际strict_load.exit=0和完整receipt收尾。进程匹配已改为精确Python命令，避免把screen父进程计入。17:58:24再次复验远端SHA后仅删本地传输副本，释放829830168字节，远端父权重保留。

将覆盖审计从单场景扩展到16个不同物理训练场景：对既定fit场景排序、等间隔取16场景、各取首条表达，不按难度/GT/分数筛选。场景为scene0000/0043/0090/0132/0175/0219/0266/0311/0364/0404/0449/0487/0534/0582/0638/0706。每场景分别使用无增强、原生增强，模型固定官方Scan parent并置eval；基础seed2027、确定性逐行输入随机状态，未重新训练或搜索seed。总32条，4次模型forward，0更新/正式行；18:05:06完成，耗时88.57秒，父模型状态逐项未变。它不是全数据集发生率、正式精度或严格同点刚体变换的因果配对。

首个诊断v1在第一次forward后因脚本错误假设“所有raw预测尺寸>0”停止，日志完整保留，0更新。查现有native_root_rec_readout与独立evaluator审计，两者都对尺寸clamp(min=1e-6)。v2仅修正诊断IoU的同一处理并记录原始非正尺寸候选数；未改模型、网络输出或评估规则。此次失败前向1次与成功v2四次分别记录，不隐去工程失败。两条件各4096原始候选中分别1149/1175个至少一轴非正，这是未筛选raw候选状态，不等于所选框全错。

实际VSA支持：无增强十组均0/16384空；增强后raw两半径仍0空，x_conv1的0.1/0.2米为1926/1616空，x_conv2的0.2/0.4米为1614/836，x_conv3的0.4/0.8米为805/332，x_conv4的0.8/1.6米为318/118。增强条件80万点中100877点在体素范围外（12.609625%），无增强41点；24932个目标点中增强后704个在范围外、无增强0个，涉及5个目标。例scene0404_00目标113点中79点越界。这些原始点仍存在，不表述成整个模型看不见，也不把范围截断全部等同于最终REC失败。

候选中心CPU几何另列：每条件按固定Query索引抽512框，无增强/增强的x_conv1 0.2米空邻域为145/146；其中37/31个候选本身IoU>0.25。两个条件的GT框中心也各有3/16在该半径内无支持。粗尺度x_conv3 0.8米空为4/8；细尺度空而粗尺度非空为141/138。故“框中心局部空”不能当坏框标签，不能以此监督质量头或屏蔽整个候选。

对准确来源文件核实，QueryAndGroup在MLP前清零空行，但StackSAModuleMSG在MLP、BN、max-pool后不再应用mask。用实际Scan权重在CPU eval下输入零组做十个MLP阶段探针，所有尺度仍有部分非零通道；x_conv1 0.2米为29/64通道非零，最大绝对值1.94428。这里0完整模型forward、10阶段forward、0更新，模型状态未变；不是train-mode BN统计或真实场景逐项激活采集，非零值也可能是学习到的缺失状态，不称为算子数值错误。

已实现最小控制models/pvground_empty_pool_mask.py：保留ball_query实际empty mask，仅在原MLP/max-pool后、分源融合前逐半径清零；保留原groupers/mlps、参数和全部state key。禁用控制调用原StackSAModuleMSG路径。它不含学习式coverage权重、不增加参数、不覆盖预测答案，不能作为复杂覆盖模型或新性能成果。源自OpenPCDet固定commit233f8498，许可证及来源归档。

验证一：真实CUDA已知非空/空双查询，禁用控制与原模块精确一致；启用后非空输出精确一致、空输出全0；仅空输出loss的梯度全0，非空特征梯度有限且非零，state键和值不变。验证二：已有全部VSA邻域非空的8条固定输入，完整Scan父模型依次禁用/启用/禁用、每次重置同seed，全部trace张量逐项一致；共3次forward、0更新/正式行。结果只证明该非空批次的行为一致，不外推6887或9508全部相同。

下一步先将这个最小控制接入完整native GT损失与训练，检查真实反向和预算，再按同父权重、同fit/holdout、同seed2027及固定训练预算与已完成native控制比较。先回答明确空邻域处理有没有收益；只有该控制和普通读取关系清楚后，再加学习式观测权重或语义/几何不同记忆，避免把基本mask作用归给复杂机制。当前尚未启动这项训练；现有纯微调负结果不改判，V99正式最好不变，Nr/Sr训练仍待Scan正式达线。此计划不恢复LR/epoch/seed扫描，不重启已结束任务。
