{
  "initial_patch_attempt": "Guard stopped because proposal and decoder both contain image projection; no source written until all replacements verified. Restricted replacement to decoder prefix.",
  "cpu_fixture_repairs": [
    "Native GMA requires 256 queries; changed CPU fixture from 16 to the real 256.",
    "Strict recovery uses fresh decoder constructor, not deepcopy of a non-leaf transient semantic output."
  ],
  "model_optimizer_steps_before_queue": 0,
  "final_cpu_checks": "passed; no scene accuracy claims"
}