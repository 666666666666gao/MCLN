# 原生首批反向预热回归

五次原生诊断显示A首次反向与所有后续反向在x_mask六张量有差异，最大绝对差3.59416e-5；
后续四种运行的GPU范数完全相同，前向输出和损失始终逐位相同。尚未确定底层具体kernel，
不把该现象归因为新稀疏模块。

本次用同首批4条fit与全部锁定输入，仅执行原生A预热、原生A正式检查、B稀疏零残差
检查，3forward/3backward，0优化器/holdout/formal。省去第二份模型的原生先行调用，
直接验证最小预热方案。后两者所有前向输出/损失/GPU共享梯度范数必须仍逐位相等，
并额外逐元素检查共享梯度atol1e-6/rtol1e-5；阈值沿用上一诊断预先登记值，不放宽。

结束核对全部参数/buffer/输入/源码/data/parent不变并保存梯度实物。预计2–4分钟，
首次180秒后观察。通过后才给新版本配对启动增加一次原生预热；原V1失败目录保持原样，
网络结构、学习率、6687步预算、科学质量门和正式输出规则均不变。
