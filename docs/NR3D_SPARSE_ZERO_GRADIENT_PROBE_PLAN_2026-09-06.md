# 稀疏局部记忆首批零残差梯度诊断

V1在首批共享梯度范数逐位相等检查退出，controller1、0优化器更新、0留出评估。
失败目录保持原样。先用原首批4条fit表达[0,1,3,4]复核M3点hash，比较5次原生forward/backward：
A原生首遍、A原生重复、B同权重原生、B真实稀疏零残差、B仅增加可微零张量。
前两项区分重复数值差异和模型副本差异；最后一项区分增加特征梯度路径与稀疏编码本身。

同parent/source/runtime，原生16个Mask参数启用梯度，其余及buffer冻结，0优化器。
记录全部原生输出是否精确相同、完整逐元素共享梯度、GPU与CPU范数、最大绝对差、
相对L2差及预先指定atol1e-6/rtol1e-5的诊断比较。该数值比较尚不替代原启动断言，
不改任何性能门槛或训练预算。全部梯度实物保存在独立服务器文件并记录SHA。

仅首批4条fit，5次forward/5次backward，0holdout/formal/optimizer。预计2–4分钟，
首次180秒后观察。结束核验两份原模型、addon、输入、源码、data、parent不变。
