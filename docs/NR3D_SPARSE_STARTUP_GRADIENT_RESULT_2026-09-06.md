# 稀疏局部记忆启动退出：V1/V2失败与完整范围诊断

**最新状态（06:47:33实查）：V2仍在共享GPU梯度范数逐位相等断言处退出，controller1、
PID27756已消失、GPU1MiB，0优化器更新、0留出评估。一次原生预热没有解决完整启动问题。**
warmup.json的4份输入点hash与前序回归相同，损失仍为16.18368148803711。
下文06:42的“完整首批待确认”是历史状态，不能再作为当前状态使用。

06:54:01启动单独完整初始化诊断PID28034，manifest
`6c58447ef971e564c07516441e8b5c93fe0a3b09068bee80842f9b2962dc12e5`。
它保留V2完整32919条数据/两臂模型/优化器/evaluator初始化，执行5次零更新反向，
保存逐元素梯度，比较原生自身、稀疏自身和跨臂差异。原atol1e-6/rtol1e-5及GPU范数
精确相等均作为报告项，不在诊断中改变检查标准。当前没有V3训练或新的科学结果。

以下为V1及缩小诊断的历史证据。

V1（manifest aa32b731…）在首批共享GPU梯度范数严格相等的断言处退出，controller1、
0优化器更新、0留出评估。不是该结构的质量FAIL；原目录、脚本、计划、错误栈均保留。
06:17:42已确认PID27206消失、GPU1MiB；原observer06:29再次确认终止，没有自动重启。

独立4条fit输入与M3点hash相同，依次执行A原生首遍、A原生重复、B同权重原生、
B真实稀疏零残差、B仅添加可微零张量，共5次forward/backward、0更新。

- 5次前向Box/score/Query Mask/Text Mask/alpha及损失均逐位相同；损失16.18368148803711。
- 原生A自己首遍与后续运行在x_mask六张量有差异，最大绝对差3.594160079956055e-5；
  其余10个共享张量精确相同。最大相对L2差约1.94e-5，未通过预设逐元素1e-6/1e-5检查。
- 后续4次GPU梯度范数全部精确相同。独立读取27,002,485-byte梯度文件复算，相对A重复，
  B原生/稀疏零残差最大逐元素差均1.49e-8，可微零张量为2.98e-8，均通过同一检查。

该证据把启动风险定位到原生反向数值行为，未定位到底层具体kernel，也不证明每次
首次反向都会出现差异。PyTorch官方数值说明只支持一般的浮点运算顺序/逐位一致性
边界，不能替代本次原Torch1.10环境实测：[官方说明](https://docs.pytorch.org/docs/2.14/notes/numerical_accuracy.html)。

随后另开进程，以同4条输入执行最小3次回归：A原生预热、A原生检查、B稀疏零残差检查，
省去B的原生先行运行。后两者前向/损失/GPU范数仍逐位相同，逐元素最大差2.98e-8，
预设atol1e-6/rtol1e-5通过；所有参数/buffer和输入恢复，0更新。16,201,557-byte梯度
实物独立复算通过。这次进程的首次反向也仅有2.98e-8差异，未再次出现前一进程较大的
首次差异；因此只记录可复现的工作流程，不宣称已经找到了确定的底层根因。

V2的最小修正：首批先做一次原生forward/backward，清空梯度、0更新，再执行原检查。
原“共享GPU梯度范数严格相等”断言保留，并增加逐元素1e-6/1e-5检查及warmup回执。
原生loss精确相等、冻结state、完整6172起点身份核验也保留。没有修改网络模块、
输入、学习率、6687步训练预算、科学质量门、候选过滤或正式输出规则。

V2在8项测试、612源码/1026数据、原权重与预热回归回执核验后，于06:37:32启动
PID27756，目录/root/autodl-tmp/mcln_sparse_point_pair_20260906_v2。启动前GPU1MiB。
完整原始启动范围中的首批检查仍待运行确认，不能以4条缩小回归替代它。安排06:47:32
检查启动完整性，06:57:32按阶段实测速估计剩余时间，再接近预计结束时240秒观察。
当前没有训练后质量或正式指标；三个基准保护结果不变，完整目标继续active。

5次诊断manifest：`62e8ea11ebc1d2b3e2e7f2b84a3e50cf1095caf3b077299879dac522c1929ccb`；梯度实物SHA：`ee337e1efca8352353d20a309939353b49c0543812dad787d8d831ba39a6f5ca`。
3次回归manifest：`8ebabc0e24661159c2bd976079a29f8adbc71ea94459e62126fed32300e178eb`；梯度实物SHA：`226409d8fe68a66b2c4b857d93d7af6022a7b7c9ac68d141334f669813188525`。
V2训练manifest：`d0b67ec35ac7645c5962a7e0379bdc15d2bc0e3d0a5f4670e276c8bd281708e4`。
脚本probe_nr3d_sparse_zero_gradients.py/check_nr3d_sparse_warmup.py为明确的诊断/回归入口，
没有将可微零张量诊断路径接入实际训练。证据见对应sparse_zero_grad_probe、
sparse_warmup_regression和sparse_point_pair_v1/v2目录。
